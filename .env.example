# ============================================
# TRADING PLATFORM ENVIRONMENT VARIABLES
#
# Design Philosophy:
# - Service configs (host, port, topics, buckets) → config/providers/*.yaml (public, versioned)
# - Secrets (passwords, tokens, keys) → .env (gitignored)
#
# This file contains ONLY secrets and environment-specific settings
# ============================================

# ============================================
# ENVIRONMENT
# ============================================
ENVIRONMENT=local  # local, dev, staging, production
LOG_LEVEL=INFO

# ============================================
# CLOUD PROVIDER
# ============================================
# Determines which client implementations to use (factory pattern)
# - opensource: Kafka, local services
# - localstack: AWS services via LocalStack
# - aws: Production AWS
# - gcp: Google Cloud Platform (Phase 6)
# - azure: Microsoft Azure (Phase 6)
CLOUD_PROVIDER=opensource  # opensource, localstack, aws, gcp, azure

# ============================================
# AWS CREDENTIALS (Secrets)
# ============================================
# For LocalStack: use 'test' credentials
# For AWS: use real credentials from IAM
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=test
AWS_SECRET_ACCESS_KEY=test
AWS_ENDPOINT_URL=http://localhost:4566  # Set for LocalStack, leave empty for AWS

# LocalStack Pro Auth Token (get from: https://app.localstack.cloud/)
# Only needed for LocalStack Pro features
LOCALSTACK_AUTH_TOKEN=your-localstack-auth-token-here

# ============================================
# DATABASE PASSWORDS (Secrets)
# ============================================
# Service configs (host, port, db name) are in config/providers/databases.yaml
CLICKHOUSE_PASSWORD=trading_pass
POSTGRES_PASSWORD=trading_pass
REDIS_PASSWORD=  # Optional - leave empty for local dev

# ============================================
# GRAFANA CREDENTIALS (Secrets)
# ============================================
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=admin

# ============================================
# EXCHANGE API KEYS (Secrets - Phase 2+)
# ============================================
# Only needed for trading execution (not market data ingestion)
BINANCE_API_KEY=
BINANCE_API_SECRET=
COINBASE_API_KEY=
COINBASE_API_SECRET=

# ============================================
# APPLICATION SECRETS
# ============================================
API_SECRET_KEY=change-me-in-production  # For JWT signing, session encryption
